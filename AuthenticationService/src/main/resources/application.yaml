server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/clan_roles_db
    username: my_db_user
    password: my_db_password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6380

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /



